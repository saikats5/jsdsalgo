function addUpTo(n){
    let total = 0;
    for(let i = 1; i <= n; i++){
        total += i;
    }
    return total;
}

function addUpTo(n){
    return n * (n + 1) / 2;
}

var t1 = performance.now();
var t2 = performance.now();

(t2-t1)/1000 //miliseconds to seconds //in chrome snippets

different machines/same machine will record different time, this is not feasible

count number of simple operations have to perform i.e., + - * / =